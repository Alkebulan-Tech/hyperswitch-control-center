// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Icon from "./Icon.bs.js";
import * as Curry from "rescript/lib/es6/curry.js";
import * as React from "react";
import * as LogicUtils from "../utils/LogicUtils.bs.js";
import * as ReactFinalForm from "react-final-form";
import * as JsxPPXReactSupport from "rescript/lib/es6/jsxPPXReactSupport.js";

function RatingOptions(props) {
  var size = props.size;
  var match = React.useState(function () {
    return false;
  });
  var setIsActive = match[1];
  var isActive = match[0];
  var ratingInput = ReactFinalForm.useField("rating").input;
  var rating = LogicUtils.getIntFromJson(ratingInput.value, 1);
  return React.createElement(
    "div",
    {
      className: "flex flex-row justify-evenly py-5",
    },
    props.icons.map(function (icon, index) {
      var iconRating = (index + 1) | 0;
      return JsxPPXReactSupport.createElementWithKey(
        index.toString(),
        Icon.make,
        {
          name: icon,
          size: size,
          className:
            isActive && rating === iconRating
              ? "rounded-full text-yellow-500"
              : "rounded-full text-gray-400 hover:text-yellow-500",
          onClick: function (param) {
            Curry._1(ratingInput.onChange, iconRating);
            Curry._1(setIsActive, function (param) {
              return true;
            });
          },
        },
      );
    }),
  );
}

var make = RatingOptions;

export { make };
/* Icon Not a pure module */
