// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Curry from "rescript/lib/es6/curry.js";
import * as React from "react";
import * as ReactFinalForm from "../../libraries/ReactFinalForm.bs.js";
import * as ReactFinalForm$1 from "react-final-form";

function FormValuesSpy$JsonBox(props) {
  return React.createElement(
    "div",
    {
      className:
        "flex-1 border border-purple-500 m-2  overflow-scroll whitespace-pre font-fira-code",
    },
    JSON.stringify(props.json, null, 2),
  );
}

var JsonBox = {
  make: FormValuesSpy$JsonBox,
};

function FormValuesSpy(props) {
  var displayProps = props.displayProps;
  var restrictToLocal = props.restrictToLocal;
  var jsonModifier = props.jsonModifier;
  var wrapperClass = props.wrapperClass;
  var wrapperClass$1 = wrapperClass !== undefined ? wrapperClass : "";
  var restrictToLocal$1 =
    restrictToLocal !== undefined ? restrictToLocal : true;
  var displayProps$1 = displayProps !== undefined ? displayProps : true;
  var subs = ReactFinalForm.useFormSubscription(["values"]);
  var canRender = restrictToLocal$1
    ? window.location.hostname === "localhost"
    : true;
  if (canRender) {
    return React.createElement(
      "div",
      {
        className: "" + wrapperClass$1 + " flex flex-col overflow-hidden",
      },
      React.createElement(ReactFinalForm$1.FormSpy, {
        children: function (props) {
          return React.createElement(
            React.Fragment,
            undefined,
            displayProps$1
              ? React.createElement(FormValuesSpy$JsonBox, {
                  json: props.values,
                })
              : null,
            jsonModifier !== undefined
              ? React.createElement(FormValuesSpy$JsonBox, {
                  json: Curry._1(jsonModifier, props.values),
                })
              : null,
          );
        },
        subscription: subs,
      }),
    );
  } else {
    return null;
  }
}

var make = FormValuesSpy;

export { JsonBox, make };
/* react Not a pure module */
