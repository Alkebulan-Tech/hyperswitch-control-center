// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Curry from "rescript/lib/es6/curry.js";
import * as React from "react";
import * as Js_exn from "rescript/lib/es6/js_exn.js";
import * as APIUtils from "../../APIUtils/APIUtils.bs.js";
import * as Analytics from "../Analytics.bs.js";
import * as LogicUtils from "../../../utils/LogicUtils.bs.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Core__Option from "@rescript/core/src/Core__Option.bs.js";
import * as HSAnalyticsUtils from "../HSAnalyticsUtils.bs.js";
import * as PageLoaderWrapper from "../../Utils/PageLoaderWrapper.bs.js";
import * as Caml_js_exceptions from "rescript/lib/es6/caml_js_exceptions.js";
import * as JsxPPXReactSupport from "rescript/lib/es6/jsxPPXReactSupport.js";
import * as UserJourneyAnalyticsEntity from "./UserJourneyAnalyticsEntity.bs.js";

function UserJourneyAnalytics(props) {
  var match = React.useState(function () {
    return /* Loading */ 0;
  });
  var setScreenState = match[1];
  var match$1 = React.useState(function () {
    return [];
  });
  var setMetrics = match$1[1];
  var metrics = match$1[0];
  var match$2 = React.useState(function () {
    return [];
  });
  var setDimensions = match$2[1];
  var fetchDetails = APIUtils.useGetMethod(undefined, undefined);
  var loadInfo = async function (param) {
    try {
      var infoUrl = APIUtils.getURL(
        /* ANALYTICS_USER_JOURNEY */ 8,
        /* Get */ 0,
        Caml_option.some(UserJourneyAnalyticsEntity.domain),
        undefined,
        undefined,
        undefined,
        undefined,
        undefined,
        undefined,
      );
      var infoDetails = await Curry._1(fetchDetails, infoUrl);
      Curry._1(setMetrics, function (param) {
        return LogicUtils.getArrayFromDict(
          LogicUtils.getDictFromJsonObject(infoDetails),
          "metrics",
          [],
        );
      });
      Curry._1(setDimensions, function (param) {
        return LogicUtils.getArrayFromDict(
          LogicUtils.getDictFromJsonObject(infoDetails),
          "dimensions",
          [],
        );
      });
      return Curry._1(setScreenState, function (param) {
        return /* Success */ 1;
      });
    } catch (raw_e) {
      var e = Caml_js_exceptions.internalToOCamlException(raw_e);
      if (e.RE_EXN_ID === Js_exn.$$Error) {
        var err = Core__Option.getOr(e._1.message, "Failed to Fetch!");
        return Curry._1(setScreenState, function (param) {
          return /* Error */ {
            _0: err,
          };
        });
      }
      throw e;
    }
  };
  var getUserJourneysData = async function (param) {
    try {
      Curry._1(setScreenState, function (param) {
        return /* Loading */ 0;
      });
      return await loadInfo(undefined);
    } catch (raw_e) {
      var e = Caml_js_exceptions.internalToOCamlException(raw_e);
      if (e.RE_EXN_ID === Js_exn.$$Error) {
        var err = Core__Option.getOr(e._1.message, "Failed to Fetch!");
        return Curry._1(setScreenState, function (param) {
          return /* Error */ {
            _0: err,
          };
        });
      }
      throw e;
    }
  };
  React.useEffect(function () {
    getUserJourneysData(undefined);
  }, []);
  var tabKeys = HSAnalyticsUtils.getStringListFromArrayDict(match$2[0]);
  var tabValues = tabKeys.map(function (key, index) {
    return {
      title: LogicUtils.snakeToTitle(key),
      value: key,
      isRemovable: index > 2,
    };
  });
  var title = "Know your users";
  var subTitle =
    "User analytics is a level deeper into payment analytics and aims at providing you a wholesome understanding of the end users and their usage patterns.";
  return React.createElement(
    "div",
    undefined,
    React.createElement(PageLoaderWrapper.make, {
      children: Caml_option.some(
        JsxPPXReactSupport.createElementWithKey(
          "UserJourneyAnalytics",
          Analytics.make,
          {
            pageTitle: title,
            pageSubTitle: subTitle,
            startTimeFilterKey: HSAnalyticsUtils.startTimeFilterKey,
            endTimeFilterKey: HSAnalyticsUtils.endTimeFilterKey,
            chartEntity: {
              default: UserJourneyAnalyticsEntity.paymentChartEntity(tabKeys),
              userPieChart: UserJourneyAnalyticsEntity.userChartEntity(tabKeys),
              userBarChart:
                UserJourneyAnalyticsEntity.userBarChartEntity(tabKeys),
              userFunnelChart:
                UserJourneyAnalyticsEntity.userJourneyFunnelChartEntity(
                  tabKeys,
                ),
            },
            defaultSort: "total_volume",
            tabKeys: tabKeys,
            tabValues: tabValues,
            initialFilters: HSAnalyticsUtils.initialFilterFields,
            initialFixedFilters: HSAnalyticsUtils.initialFixedFilterFields,
            options: HSAnalyticsUtils.options,
            getTable: function (param) {
              return [];
            },
            colMapper: function (param) {
              return "";
            },
            deltaMetrics: HSAnalyticsUtils.getStringListFromArrayDict(metrics),
            deltaArray: [],
            singleStatEntity:
              UserJourneyAnalyticsEntity.getSingleStatEntity(metrics),
            filterUri:
              "" +
              window._env_.apiBaseUrl +
              "/analytics/v1/filters/" +
              UserJourneyAnalyticsEntity.domain +
              "",
            tableGlobalFilter: HSAnalyticsUtils.filterByData,
            moduleName: "UserJourney",
          },
        ),
      ),
      screenState: match[0],
      customUI: Caml_option.some(
        React.createElement(HSAnalyticsUtils.NoData.make, {
          title: title,
          subTitle: subTitle,
        }),
      ),
    }),
  );
}

var make = UserJourneyAnalytics;

export { make };
/* react Not a pure module */
